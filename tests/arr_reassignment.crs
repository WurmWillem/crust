fn print_arr(int[] arr) {
    // TODO: update this to use len()
    for i in 0 to 3 {
        print(arr[i]);
    }
}

fn main() {
    int[] x = [1, 2, 3];
    print_arr(x);

    x[0] = 2;
    x[1] = 4;
    x[2] = 8;

    print_arr(x);
}
